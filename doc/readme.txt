以 doc/PROTOTYPE_DESIGN-V2.md 为设计规则源，以 doc/ui_style_preview (2).html 为视觉和组件参考；二者冲突时以前者为准。
2、本次重构以统一视觉系统、页面框架和组件样式为主，不改变现有业务范围、功能入口、字段、操作。
3、允许根据业务关系合并页面、页签、弹窗或抽屉，但不得以合并为目的删减功能；原有页面入口和跨页面链接必须继续可访问，或准确映射到新页面的对应状态。
 4、页面允许因业务内容不同采用列表、详情、仪表盘或配置页布局，但全局框架、视觉语言、组件行为和状态语义必须一致。
5、页面的组件不要有太多的组件说明，