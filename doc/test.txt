1\sys_model_repo.html 模型部署服务的弹窗，服务类型按照横向排版， 同时增加镜像的选择，支持两种方式，一种是填写路径，二是直接下拉获取镜像仓库中的镜像
sys_task_list.html 我的任务页面，修改
sys_model_training.html 模型训练页面


一、index.html 系统概览页面
1、全域 QPS的单位改成：req/5min 

二、算力调度页建议，sys_dispatch.html
1、调度域、时间范围
1、指标卡片：硬件资源、实例数、底层实例积压、总调用数并说明近5min的统计数（每个卡片区分在线和离线）
2、总的算力调度页面，区分在线和离线两个页签
2、离线页签
2.2 折线图：输出吞吐、任务实时算力分配占比、请求数趋势图（不同的曲线代表：成功请求、失败请求、拒绝请求）
2.3  任务列表：名称修改成“任务列表”，字段，增加成功调用数
3、在线页签：
3.2 折线图：输出吞吐、任务实时算力分配占比、请求数趋势图（不同的曲线代表：成功请求、失败请求、拒绝请求）
2.3  任务列表：名称修改成“任务列表”，字段增加成功调用数、去掉“调度模式”和“当前算力权重”，将操作的配置算力，修改成查看算力，可以弹窗显示，服务实例数、以及运行实例的列表（字段：实例名称、节点、显卡）
1、删掉sys_api_key_mgmt.html请求健康页面
2、sys_model_monitor.html
1）页面选中模型后，
性能与容量诊断修改成“模型性能核心指标”，只保留选中的模型，并区分在线online和离线offline,成为2个横向卡片
2）增加页面“算力资源情况”：内容为“按模型服务统计”页签下的Pod 资源使用趋势（KV Cache & GPU）。会有多个pod会有多个折线图，折线图增加序号，折线图名称为节点ip，节点ip下方显示实例名称。
3）算力资源情况：支持按照模型服务名称筛选和节点ip筛选
4）按任务统计，去掉任务tag字段。同时要支持在线和离线任务的筛选。增加操作列，详情，点击可以进入任务详情。
5）任务详情的侧边栏，请补充完整。并按照图片中的调用信息挪到我的任务列表，右上角点击后以抽屉的形式展示。 任务详情保留 任务统计指标 、请求趋势、请求日志（请求日志支持按照状态筛选）
------
1、sys_task_detail.html的请求日志的输入和输出比较长，请改成点击弹窗或者抽屉的格式
2、sys_task_detail.html 去掉所有的H2说明，只保留H2标题。
3、任务统计指标时间筛选框和sys_dispatch.html的时间范围筛选样式保持一致
4、 支持在线测试的页面，模拟postman,自动填充好请求体（支持编辑），点击测试发送请求，并返回请求结果
---


1、sys_model_monitor.html的模型服务统计，列表增加服务类型字段，
2、sys_model_monitor.html 增加H2资源使用，H2标题右侧在线和离线的切换页签，可以和sys_dispatch.html保持一样的切换页签，去掉这个页面所有H2标题名称下的装饰说明
3、去掉sys_model_monitor.html按任务统计的任务类型筛选，并且去掉“关联任务”这个标题
4、sys_task_list.html的调用信息，用辅助颜色突出。去掉当前任务和下方的关联模型、任务类型、服务状态，增加下拉模型类别，分别由通用大模型/v1/chat/completions，语音转写模型/asr/recognition， 小模型/SmallModel/, apikey调用说明支持Authorization: Bearer的请求头方式，和支持请求体“apikey”的方式， 调用示例，去掉node.js, 支持postman的调用示例。 支持apikey和调用示例的复制
---
1、sys_dispatch.html 只保留H2标题，去掉H2的说明
2、sys_task_detail.html 任务统计指标，增加统计按钮，选择按天和时间范围，按天列出统计输入tokens总数、输出tokens总数、请求总数、请求成功数、请求失败数、请求拒绝数，
3、sys_task_list.html 去掉页面上方的指标卡片
4、sys_task_list.html的任务列表，字段修改成序号、名称、模型、状态、类型、APIkey、申请人、创建人、创建时间。 运行中的任务操作项由详情、停止， 停止的任务，操作项有删除、编辑、详情、启动

----
2、sys_task_detail.html的任务指标增加“请求中数”、
sys_task_list.html 列表筛选项，增加“模型”筛选项
3、sys_model_training.html 去掉toolbar-card部分，在心间任务左侧增加任务名称的搜索栏即可。
任务卡片视图， 去掉训练进度、失败原因、最佳训练结果等展示、去掉算力资源、去掉查看日志和重新训练按钮，任务详情修改成“训练版本”

--
sys_model_monitor.html的时间范围的选择，请按照doc/PROTOTYPE_DESIGN-V2.md下， 精确绝对时间段选择器的规范

index.html 增加任务算力占比
目的是按照模型以及服务类型（在线、离线）来区分，不同的任务的输出tokens算力占比，清晰显示哪个任务用得比较多

sys_model_service.html
同样、去掉所有指标卡片下方的小字

sys_model_governance.html 修改这个模型纳管流程，将“**申请线下验证服务器**”增加“可选标识”。将“申请平台部署资源”改成“申请账号部署权限和平台资源”
2、 按照请按照doc/PROTOTYPE_DESIGN-V2.md下， 修改“申请线下验证服务器”和“申请平台账号和资源”的弹窗内容
3、“申请线下验证服务器” 的表单内容，去掉服务器使用开始时间 、服务器使用结束时间 ，改成精确绝对时间段选择器“资源使用时间”、
4、去掉“平台部署前置材料”，
5、我的部署资源申请，保留目前的空状态的显示，同时增加有列表字段的显示。 去掉“进入授权审批”的按钮， 可以将空置显示用按钮切换，方便原型交互


sys_resource_approval.html
1、 原型说明不准确，去掉“原型说明”
2、按照doc/PROTOTYPE_DESIGN-V2.md，优化列表组件
3、 审核和查看申请，改成弹窗显示，不要用右侧抽屉的交互方式。去掉“登录账号 / 方式 *”。并在列表段，审批通过的，显示授权的服务器ip和资源型号*卡数
4、index.html去掉“集群节点状态”部分的内容
5、新增账号授权， 授权集群之后，授权集群改成下拉选择的交互方式，参考部署模型的弹窗，选择之后，会显示显卡信息。 并且支持勾选，指定集群，指定节点两种方式，当选择指定节点后，下拉选择具体的ip.
6、账号授权列表，也要显示已授权的资源，能在列表中展示

sys_resource_approval.html
1、调整新增授权的弹窗大小，目前显示不全
2、新增授权，授权账号除了下拉筛选，需要支持搜索。去掉资源型号 和授权卡数
3、sys_resource_approval.html的列表标题和规范是否不一致，目前列表标题高度过高
5、 将试用资源修改成“线下验证服务器”、“正式资源”需修改成“平台账号资源”
6、  审批工作台的列表筛选项，保留类型和状态，同时列表的规范按照doc/PROTOTYPE_DESIGN-V2.md的列表带多维筛选框规范。

1、sys_model_governance.html部署资源申请的列表，按照doc/PROTOTYPE_DESIGN-V2.md的列表带多维筛选框的规范执行，
列表字段保留 申请名称、类型、业务系统、申请人、资源需求、状态、更新时间、
2、 查看的弹窗页按照规范重新绘制
3、sys_resource_approval.html 审批工作台的列表字段，申请名称、类型、业务系统、申请人、状态、授权集群（如果是线下服务器，则显示-）、授权IP
账号授权的列表字段，修改成授权账号、授权集群、授权IP、授权时间、状态。操作去掉查看
4、sys_resource_approval.html的“平台账号和资源”的审核弹窗，按照规范重新绘制。弹窗除了申请的信息之外，也需要体现像授权弹窗的表单内容，如授权集群、点击集群后显示显卡信息，选择授权信息等。

1、sys_resource_approval.html的授权弹窗，点击授权集群后，显示可授权节点列表页，替换成sys_model_repo.html的显卡详情列表。

1、sys_model_governance.html的“申请账号部署权限和平台资源”，修改成“申请平台权限和资源”
2、申请线下验证服务器的弹窗，去掉模型名称 。
3、申请平台权限和资源，去掉“账号、部署权限与上线信息”、保留“是否完成线下验证”、“预计并发”、“计划上线时间”、资源需求

1、sys_resource_approval.html的资源申请列表，增加
2、sys_model_service.html的模型服务列表，列表字段改成：服务名称、模型、服务类型、状态、实例数、部署地址、部署节点，操作项。 停止中的操作项有删除、编辑、启动。运行中的操作项有扩缩容、停止。列表页需要分页。 
3、 指标卡片为 模型服务数量、模型数、运行中/ 全部


4、sys_resource_approval.html的列表，增加类型是“平台账号和资源”数据，并在这个数据的操作项，有审核弹窗。 审核弹窗和“线下验证服务器”的弹窗不一样。除了申请的信息之外，也需要体现像授权弹窗的表单内容，如授权集群、点击集群后显示显卡信息，选择授权信息等
5、所有的侧边栏，去掉sys_model_governance.html模型纳管页面。只保留资源申请

1、sys_model_monitor.html的时间筛选组件，请按照doc/PROTOTYPE_DESIGN-V2.md下， 精确绝对时间段选择器的规
